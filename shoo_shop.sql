CREATE TABLE shoos(
  id serial8 primary key,
  first_name varchar(255),
  last_name varchar(255),
  address varchar(255),
  city varchar(25),
  style varchar(25),
  size varchar(2),
  quantity int2    
);
